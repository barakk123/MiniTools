#ifndef MINITOOLS_H
#define MINITOOLS_H

// Function to prompt the user for a valid integer input and return the input value.
// This function ensures the input is a single integer with no extra characters (e.g., spaces or other numbers).
// It will repeatedly prompt the user until valid input is received.
int get_valid_input(void);

#endif
